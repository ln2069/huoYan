services:
  frontend:
    build: .
    container_name: huoyan_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    image: huoyan_backend:latest
    container_name: huoyan_backend
    ports:
      - "8000:8000"
